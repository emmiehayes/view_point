<section class="hero">
  <div class="search-box">
    <%= form_tag :search, method: :get do %>
      <div class="control">
        <%= text_field_tag :movie, placeholder="enter a movie..." %>
        <br>
        <%= text_field_tag :zipcode, placeholder="enter your zipcode" %>
      </div>
      <div class="control">
        <%= submit_tag 'Submit', class: "button is-dark is-large" %>
      </div>
    <% end %>
  </div>
</section>
